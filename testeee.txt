// 1. Garanta que importou o useState aqui também!
import { useState } from "react";

// ... dentro da função Login ...
return (
  <form onSubmit={handleLogin}> {/* 2. O handleLogin PRECISA estar aqui */}
    <input 
      type="email" 
      onChange={(e) => setEmail(e.target.value)} // 3. O setEmail PRECISA estar aqui
    />
    <input 
      type="password" 
      onChange={(e) => setPassword(e.target.value)} // 4. O setPassword PRECISA estar aqui
    />
    <button type="submit">Entrar</button>
  </form>
);